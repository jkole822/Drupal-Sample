<a class="bg-blue-500 cursor-pointer font-bold inline-block outline-hidden px-3 py-2 ring-blue-300 rounded-lg text-center text-white transition-all focus-visible:ring-3 hover:opacity-80 {{ class ?? '' }}"
   href="{{ href }}"
   target="{{ target ?? '_self' }}"
>
  {{ title }}
</a>
